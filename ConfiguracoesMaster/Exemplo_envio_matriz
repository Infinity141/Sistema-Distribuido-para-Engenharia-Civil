import numpy as np
from config_cluster import tarefa_cluster

@tarefa_cluster
def processar(matriz):
    return np.sum(matriz)

# O Master carrega o ficheiro do seu próprio disco para enviar
matriz_do_arquivo = np.load('minha_matriz.npy')

# Envia os dados carregados pela rede
job = processar.delay(matriz_do_arquivo)
